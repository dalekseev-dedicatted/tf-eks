apiVersion: v1
kind: Service
metadata:
  name: cd-api
  namespace: cd-dev
  labels:
    app: cd-api
    environment: dev
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: cd-api
